import { FestivalSearchPipe } from './festival-search.pipe';

describe('FestivalSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FestivalSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
